OPENAI_API_KEY=
COPILOT_MODEL=openai/gpt-5.5
# The Fizzy Maps MCP app from apps/map-app. Point this at a deployed copy to demo against the cloud.
NEXT_PUBLIC_MAP_MCP_URL=http://127.0.0.1:3001/mcp
# Optional bearer token for a deployed map app. Only ever sent to NEXT_PUBLIC_MAP_MCP_URL.
MCP_AUTH_TOKEN=
